import { fetchDataService } from '../server-src/service/fetchDataService';

import { expect } from 'chai';
import { userService } from '../server-src/service/UserService';

describe.only('GuildService tests:', async function () {
	it('first test', async function () {
		// const result = await userService.getUser({
		//     allyCode: 1111,
		//     discordId: 23232
		// });
		// let player = await fetchDataService.getPlayer(621723826);
		// console.log('Player ', player);
		// expect(result).eq(null);
	});
});
